const badge = require('../../src/badge');

describe('Badge module', function() {
    it('Creator set badge');
    
    it('Fail if creator id is not present');
    
    it('Fail if creator id is not a number');
});
